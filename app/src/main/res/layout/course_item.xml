<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="75dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <WebView
        android:id="@+id/course_item_image_view"
        android:layout_width="55dp"
        android:layout_height="55dp"
        android:src="@android:drawable/alert_dark_frame"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:backgroundTint="#000"
        app:layout_constraintLeft_toLeftOf="parent"
        android:layout_marginLeft="8dp"/>

    <TextView
        android:id="@+id/course_item_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Course Label"
        android:textSize="32dp"
        app:layout_constraintTop_toTopOf="@id/course_item_image_view"
        app:layout_constraintBottom_toBottomOf="@id/course_item_image_view"
        app:layout_constraintLeft_toRightOf="@id/course_item_image_view"
        android:layout_marginLeft="16dp"
        android:textColor="#000"/>


    <CheckBox
        android:background="@drawable/favorite_state"
        android:id="@+id/course_item_favorite_image_view"
        android:layout_width="35dp"
        android:layout_height="35dp"
        app:layout_constraintTop_toTopOf="@id/course_item_image_view"
        app:layout_constraintBottom_toBottomOf="@id/course_item_image_view"
        app:layout_constraintRight_toRightOf="parent"
        android:layout_marginRight="16dp"
        android:button="@null"/>

</android.support.constraint.ConstraintLayout>